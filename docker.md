In this branch,
I built a dockerfile that creates a docker image from the local application.
This way, you can run the application using a container and it is much more flexible,
and this will help me later on to integrate it with k8s.


